class Calculator {
  addNumbers(a, b) {
    return a + b;
  }

  subtractNumbers(a, b) {
    return a - b;
  }
}

const calculator = new Calculator();

const result = calculator.multiplyNumbers(5, 10);
